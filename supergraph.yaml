federation_version: =2.3.2
subgraphs:

  films:
    routing_url: https://localhost:3000
    schema:
      file: ./musician.graphql

  instrument:
    routing_url: https://localhost:3001
    schema:
      file: ./instrument.graphql

  album:
    routing_url: http://localhost:3002
    schema:
      file: ./album.graphql
      